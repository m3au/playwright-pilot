[install]
production = false
frozenLockfile = false
autoInstall = false

[tools]
typescript = true

[registry]
default = "https://registry.npmjs.org"

[runtime]
env = { NODE_ENV = "development" }

[test]
coverage = true
# Only scan for tests in the tests/unit directory
root = "tests/unit"
